# Session Opener — Read Before Touching Anything

You are starting a new Claude Code session on the Potheads pottery tracker app.

**Before asking what to do, complete these steps in order:**

1. Read `/CLAUDE.md` in full.
2. Read `DECISIONS.md` if it exists. If it doesn't exist, note that it hasn't been created yet.
3. Run a file tree scan of `src/` (pages, components, lib) and `public/`. Note any files that weren't in the Current App State table in CLAUDE.md — those are new since last update.
4. Write a short status summary (3–5 bullets) covering:
   - What the app currently does end-to-end
   - Any files that look partial, stubbed, or unused
   - Any obvious gaps between what CLAUDE.md describes and what actually exists
5. Then ask: **"What's the goal for this session?"**

Do not propose any changes, write any code, or make any assumptions about what needs to be done until the user answers that question.
